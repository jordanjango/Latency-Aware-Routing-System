package com.example.LAAR;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
// This is the parent folder for the java application or spring boot application
@SpringBootApplication
public class LaarApplication {
	public static void main(String[] args) {
		SpringApplication.run(LaarApplication.class, args);
	}

}
